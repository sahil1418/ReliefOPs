# Production dependencies for Render / Cloud Run deploy.
# Slimmed: prophet, xgboost, pandas, numpy, googlemaps removed (not imported anywhere).
# Adding them back would inflate the image by ~400 MB and the Render build by ~3 min.

# Web framework
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Validation
pydantic[email]==2.9.2
pydantic-settings==2.6.0
email-validator==2.2.0

# Firebase + GCP clients
firebase-admin==6.5.0
google-cloud-firestore==2.19.0
google-cloud-pubsub==2.27.1
google-cloud-storage==2.18.2
google-cloud-secret-manager==2.21.1

# Gemini SDK
google-genai==0.5.0

# Routing solver
ortools==9.11.4210

# HTTP client + observability
httpx==0.27.2
tenacity==9.0.0
structlog==24.4.0

# Optional: SMS via Twilio (graceful no-op without creds)
twilio==9.3.5

# ML: Transit anomaly detection (Isolation Forest) + feature pipeline
scikit-learn==1.5.2
numpy==1.26.4

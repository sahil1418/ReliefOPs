/**
 * Shared shadcn/Radix component library.
 *
 * COMMIT 1: empty — components live in apps/web/components/ui until promoted here.
 * Promote when a component is consumed by 2+ apps.
 */
export {};
